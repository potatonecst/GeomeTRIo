# Gemini 開発ガイドライン (Gemini Development Guidelines)

このドキュメントは、AIアシスタント（Gemini）が本プロジェクトで開発作業を行う際のガイドラインです。

## 0. 重要事項 (Critical Instructions)

**【最優先】使用言語について:**
ユーザーとの対話、変更箇所の説明、作業計画の提示は当然として、**ツールを実行する直前の行動予定の説明（いわゆる "I will..." から始まる文章）も、必ず日本語で記述してください。**
英語での出力は、コード内のコメントや、英語での出力が自然な識別子などを除き、原則禁止とします。

## 1. プロジェクト概要

本プロジェクトは、Unity製2Dシューティングゲーム「GeomeTRIo」の開発です。
三角形の自機を操作し、発射点を切り替えながら画面下部から迫る敵を倒す、戦略的なシューティングゲームです。
詳細については `spec/` ディレクトリ内の仕様書を参照してください。

### ゲーム仕様概要
- **自機:** 正三角形の形状。前方、左斜め後方、右斜め後方の3頂点が弾の発射口となり、左右に切り替えが可能。
- **敵:** 画面下部から出現。敵同士の衝突で全方位に弾が発射されるギミックがあり、連鎖的な撃破（誘爆）を狙う戦略性がある。
- **ステータス:**
    - **HP:** 1から10の間で設定可能（初期値3）。尽きるとゲームオーバー。
    - **SP:** 0から10の間で設定可能（初期値3）。
- **特殊アクション (スピンアタック):** SPを消費して発動。無敵状態で回転しながら3方向に弾を発射する（ボムに相当）。
- **ランキング:** 各ステージごとに上位5つのスコアを記録。ハイスコア時はプレイヤーネームと共に保存される。

## 2. 基本的な開発方針

### 仕様書ファースト (Spec First)

新規機能の追加や既存機能の大幅な変更を行う際は、まず `spec/` ディレクトリ内の関連する仕様書を更新・修正してください。仕様書を基に議論や合意形成を行います。

### フォルダの役割と記述内容の区別

*   **`spec/` (仕様書):**
    *   **役割:** 「何を作るか」「どう動くべきか」を定義する場所。
    *   **記述内容:** 具体的な振る舞い、数値パラメータ、UIの挙動、データ構造の定義など。
    *   **注意:** 実装前にここを具体的に固めること。「スカスカ」な状態を避け、詳細まで詰めてから実装に移る。
*   **`docs/` (実装解説):**
    *   **役割:** 「どう実装されたか」を解説する場所。
    *   **記述内容:** *実装済み*のコードの解説、設計意図、技術的な背景（Unityの仕組みなど）。
    *   **注意:** **未実装の機能については記述しないこと。** 実装が完了した後に、そのコードに基づいて解説を追記する。

### 仕様書と実装の同期 (Sync Spec & Code)

**重要:** コードを変更・実装する際は、必ず `spec/` (仕様書) および `docs/` (技術ドキュメント) との整合性を維持してください。

1.  **仕様変更時:** まず `spec/` を更新し、その要件に基づいてコードを実装してください。
2.  **実装変更時:** リファクタリングや詳細設計の変更を行った場合、即座に `docs/` 内の該当ファイルを更新してください。
3.  **乖離の禁止:** 仕様書、実装コード、技術ドキュメントの3者が常に一致している状態を保ってください。「コードだけ直してドキュメントは後で」は禁止です。

### ドキュメント作成・更新ルール

コードに変更を加えた場合、以下の `docs/` 内のファイルもセットで更新することを義務とします。
（※具体的なファイルはプロジェクト構成に準じますが、主に `docs/architecture.md` や `docs/api_reference.md` 等を想定しています）


更新の際は、単なるコードのコピペではなく、**「変更の意図」「新しいメソッドの仕様（引数・戻り値）」「注意点」** を解説として追記・修正してください。

さらに、ユーザーの学習を支援するため、**コードの動作原理、使用している技術やパターンの背景、なぜその実装を選択したか（例：Unityのライフサイクル（Awake/Start/Update）の挙動、Quaternionによる回転処理の仕組み、オブジェクトプーリングの採用理由など）**についても、可能な限り詳細に解説を記述してください。単なる仕様の説明にとどまらず、教育的な観点からの補足説明を推奨します。

**【重要】解説の粒度について:**
ユーザーは自身でコードを記述していないため、実装の詳細がブラックボックス化することを避ける必要があります。
「どの行が何をしているか」「なぜこの関数が必要なのか」「React/Unity特有の作法はどれか」といった、コードを読み解くための補助線を引くような解説を心がけてください。

## 3. 技術スタックと規約

**主要技術:** Unity, C#

以下、Unity開発における記述例・指針
- **UI:** uGUI (Canvas) および TextMeshPro を標準として使用します。
- **データ管理:** 敵のパラメータやステージ設定などの静的データは `ScriptableObject` を活用して管理します。
- **コーディング規約:**
    - Inspectorで設定するフィールドは `public` ではなく `[SerializeField] private` を使用してください。
    - クラス名・メソッド名は PascalCase、変数名は camelCase を基本とします。
    - 非同期処理が必要な場合は、可能な限り `UniTask` (導入済みの場合) または `Coroutine` ではなく `async/await` パターンを検討してください。

## 4. Geminiへの指示

- **事前確認:** コードを変更する前に、必ず `spec/` ディレクトリ内の関連ドキュメントを確認し、プロジェクトの全体像を把握してください。
- **ドキュメントの整合性:** 機能の追加や修正が完了した際は、その内容が `spec/` 内のドキュメントに正しく反映されているかを確認し、必要であれば更新してください。
- **コードコメント:** 処理の意図や複雑なロジックについては、詳細かつ分かりやすいコメントを記述してください。
- **詳細な日本語コメントの徹底:**
    - ユーザーは実装の詳細を把握していないため、コード自体が解説書を兼ねるレベルで詳細な日本語コメントを記述してください。
    - 変数の意味、関数の目的、複雑なロジックの解説、Reactフック（useState, useEffect等）の挙動など、冗長と思われる程度に丁寧に記述することを求めます。
    - 特に、Unityとの連携部分や、非同期処理、計算ロジックについては、背景知識がなくても理解できるように補足してください。
    - **学習者への配慮:** 開発者はプログラミング学習中であることを念頭に置き、「どの関数・メソッドを呼び出しているか」「それによってどのような効果（再描画、非同期実行など）が得られるか」といった、実装の仕組みレベルの解説もコメントに含めてください。